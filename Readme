# Agent Pal

An AI-powered knowledge graph and question answering system built with Electron.

## Features (Planned)

- Question answering interface
- Data ingestion from multiple sources
- Knowledge graph integration
- Agent tools for automation

## Setup

1. Install dependencies:
   ```
   npm install
   ```

2. Run the application:
   ```
   npm start
   ```

## Project Structure

- `src/main.ts` - Main Electron process
- `src/preload.ts` - Secure context bridge
- `src/ui/index.ts` - UI entry point
- `src/ui/components/ChatBar.ts` - Chat interface component
- `src/ui/styles/main.css` - Application styles
- `src/ui/index.html` - Application UI
- `assets/` - Images and icons

## Development

This is a work in progress. Future development will include:

1. Integration with AI models
2. Knowledge graph implementation
3. Data ingestion pipelines
4. Agent tools for advanced capabilities
